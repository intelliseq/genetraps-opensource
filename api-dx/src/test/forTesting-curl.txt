
curl -XPOST "web_app:secret@localhost:9999/oauth/token" -d "grant_type=password&client_id=web_app&username=admin&password=welcome1"

##########################

TOKEN=$(curl -XPOST "web_app:secret@localhost:9999/oauth/token" -d "grant_type=password&client_id=web_app&username=admin&password=welcome1" | jq -r ".access_token")

curl -H "Authorization: Bearer $TOKEN" "localhost:9090/hello"
#should give positive results


